# ✈️ 인천⇄페낭 최저가 앱 사용법

## 이 앱이 하는 일
- **싱가포르항공·말레이시아항공·대한항공**만, **경유 1회 이하**, **경유 대기 1시간 40분~3시간**인 항공편 중에서
- **3~45일 뒤** 출발하는 날짜별 **편도 최저가**를 인천→페낭, 페낭→인천 두 방향 모두 모아서 보여줍니다.
- 매일 아침 7시쯤 **무료 조회 횟수가 바닥나지 않을 만큼만** 날짜 몇 개를 새로 확인합니다.
  (가까운 날짜일수록 더 자주 확인. 이번 달에는 남은 횟수 중 50회는 직접 검색용으로 남겨 둡니다)

## 화면 보는 법
| 부분 | 설명 |
|---|---|
| 가장 싼 날 TOP 5 | 지금까지 확인한 날짜 중 가장 싼 5일. 누르면 편명·시각·경유 대기시간 |
| 날짜별 최저가 | 달력. 숫자는 **만원** 단위. 초록일수록 쌈. 점선 칸은 아직 확인 전 |
| 가는 날 + 오는 날 조합 | 머무는 기간(3~7박 등)에 맞춰 편도 두 장 합계가 가장 싼 날짜 조합 |
| 최근 가격 변동 | 새 최저가, 5% 이상 내린 날짜 |
| 상세 창의 버튼 | 구글 플라이트·스카이스캐너·네이버에서 **그 날짜로 바로** 최종 가격 확인 |

## 켜는 방법 두 가지

### A. 내 PC로 켜기 (지금 바로 가능)
1. 이 폴더의 **`앱 켜기.bat`** 을 더블클릭
2. 검은 창에 나온 주소로 접속
   - PC: `http://localhost:3100`
   - 휴대폰: `http://192.168.x.x:3100` (창에 나온 주소, **같은 와이파이일 때만**)
3. 창을 켜 두면 매일 아침 7시 이후 자동으로 가격을 새로 확인합니다.

> 단점: PC가 꺼져 있거나 휴대폰이 밖(페낭 등)에 있으면 볼 수 없습니다.

### B. 인터넷에 올리기 — GitHub (추천, 무료)
PC를 꺼도 **매일 자동 업데이트**되고, **어디서든** 휴대폰으로 볼 수 있습니다.
1. github.com 가입 → 새 저장소(Repository) 만들기 (Public)
2. 이 `penang-app` 폴더 내용을 저장소에 올리기
3. 저장소 **Settings → Secrets and variables → Actions → New repository secret**
   이름 `SERPAPI_KEY`, 값은 `.env`에 있는 키
4. **Settings → Pages** → Branch `main` / 폴더 `/ (root)` → Save
5. **Actions** 탭 → "가격 업데이트" → **Run workflow** 로 한 번 실행
6. 몇 분 뒤 `https://내아이디.github.io/저장소이름/` 으로 접속

> 키는 Secrets 에 들어가므로 외부에 보이지 않습니다. 공개되는 것은 가격 데이터뿐입니다.

## 휴대폰 홈 화면에 추가
- **안드로이드(크롬)**: 주소 접속 → 오른쪽 위 `⋮` → **홈 화면에 추가** (또는 앱 설치)
- **아이폰(사파리)**: 주소 접속 → 아래 공유 버튼 `□↑` → **홈 화면에 추가**

## (선택) 가격이 내리면 휴대폰 알림 받기
1. 휴대폰에 **ntfy** 앱 설치 (무료, 가입 불필요)
2. 앱에서 남이 짐작하기 어려운 주제 이름 구독 (예: `penang-kim-8f3k2`)
3. 같은 이름을 `NTFY_TOPIC`으로 설정
   - PC로 켤 때: 상위 폴더 `.env` 에 `NTFY_TOPIC=penang-kim-8f3k2`
   - GitHub일 때: Secrets 에 `NTFY_TOPIC` 추가

## 조건 바꾸기
`config.json` 을 고치면 됩니다.
| 항목 | 뜻 | 지금 값 |
|---|---|---|
| airlines | 항공사 코드 | SQ, MH, KE |
| layoverMinMinutes / layoverMaxMinutes | 경유 대기 최소/최대(분) | 100 / 180 |
| daysAheadMin / daysAheadMax | 며칠 뒤 출발까지 볼지 | 3 / 45 |
| reserveSearches | 직접 검색용으로 남길 조회 횟수 | 50 |
| maxPerRun | 하루에 최대 몇 번 조회할지 | 12 |

## 알아둘 점
- 무료 요금제는 **월 250회**라서, 날짜 하나를 다시 확인하는 데 며칠씩 걸립니다.
  칸마다 "며칠 전 확인"이 표시되니, **예약 직전에는 상세 창의 구글 플라이트 버튼으로 최종가를 꼭 확인**하세요.
- 대한항공은 페낭에 직접 가지 않아 목록에 거의 나오지 않습니다 (쿠알라룸푸르 직항 KE427).
- 편도 두 장보다 **왕복**이 쌀 수 있습니다. 조합 목록의 "구글 왕복 비교" 링크로 확인하세요.
